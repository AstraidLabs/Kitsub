# Tests

Unit tests live under `tests/` with the primary suite in `tests/Kitsub.Tests`. The suite focuses on deterministic unit tests for core selection logic, tooling command construction, and JSON parsing.

## Run tests

```bash
dotnet test
```
