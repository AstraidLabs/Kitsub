# Testing Kitsub

## Unit tests
```bash
dotnet test
```

## Optional coverage
```bash
dotnet test --collect:"XPlat Code Coverage"
```

## Notes
- Integration tests are disabled by default. Enable them with `KITSUB_RUN_INTEGRATION=1`.
